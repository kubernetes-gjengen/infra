# virtualenv / pyenv
.venv
env/
.python-version

# Python bytecode
__pycache__/
*.pyc

# ansible artifacts
artifacts

# local config (see .env.example) - never committed
.env

playbooks/kubeconfig.yml
playbooks/files/k8-scheduler

# field-test logs fetched by `make collect-logs`
collected-logs/

# registry TLS key material (should be generated outside the repo, but just in case)
registry/*.key
registry/*.crt
registry/*.csr
registry/*.srl
registry/*.cnf
