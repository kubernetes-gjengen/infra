# virtualenv / pyenv
.venv
./env/
./.env
.python-version

# Python bytecode
__pycache__/
*.pyc

# ansible artifacts
./artifacts
playbooks/kubeconfig.yml
playbooks/inventories/discovered_hosts.json
playbooks/files/k8-scheduler

# field-test logs fetched by `make collect-logs`
collected-logs/

# registry TLS key material (should be generated outside the repo, but just in case)
registry/*.key
registry/*.crt
registry/*.csr
registry/*.srl
registry/*.cnf
